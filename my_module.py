# file này được đã import vào file module.py

def maxer(ls):# hàm được tạo
    return max(ls) # lists được truyền vào (ls)
# return max là gọi ra giá trị lớn nhất nằm trong lists

def miner(ls):# hàm được tạo
    return min(ls) # lists được truyền vào (ls)
# return min là gọi ra giá trị lớn nhỏ nhất nằm trong lists


def namit(name): # Hàm namer được tạo và truyền vào name
    print('hello world ' + name) 
# hello world được cộng vào phần tử 
# trong hàm namer được truyền vào name   